<!-- Validation failure messages -->
<% if @person.errors.any? %>
  <% @person.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Person #<%= @person.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_person/<%= @person.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for first_name -->
      <div class="form-group">
        <label for="first_name" class="control-label">
          First name
        </label>

        <input type="text" id="first_name" name="first_name" placeholder="First name" class="form-control" value="<%= @person.first_name %>">
      </div>

      <!-- Label and input for last_name -->
      <div class="form-group">
        <label for="last_name" class="control-label">
          Last name
        </label>

        <input type="text" id="last_name" name="last_name" placeholder="Last name" class="form-control" value="<%= @person.last_name %>">
      </div>

      <!-- Label and input for linkedin_url -->
      <div class="form-group">
        <label for="linkedin_url" class="control-label">
          Linkedin url
        </label>

        <input type="text" id="linkedin_url" name="linkedin_url" placeholder="Linkedin url" class="form-control" value="<%= @person.linkedin_url %>">
      </div>

      <button class="btn btn-warning">
        Update Person
      </button>
      or
      <a href="/people/<%= @person.id %>">Cancel</a>
    </form>
  </div>
</div>
