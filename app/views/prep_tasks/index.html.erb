<div class="page-header">
  <h1>
    Prep tasks
    <a href="/prep_tasks/new" class="btn btn-lg btn-success">New Prep task</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Belief</th>
        <th>User</th>
        <th>Target people</th>
        <th>Scheduled datetime</th>
        <th>Completion datetime</th>
        <th>Body</th>
        <th>Complete</th>
        <th>Actions</th>
      </tr>

      <% @prep_tasks.each do |prep_task| %>
      <tr>
        <td><%= prep_task.belief_id %></td>
        <td><%= prep_task.user_id %></td>
        <td><%= prep_task.target_people_id %></td>
        <td><%= prep_task.scheduled_datetime %></td>
        <td><%= prep_task.completion_datetime %></td>
        <td><%= prep_task.body %></td>
        <td><%= prep_task.complete %></td>
        <td>
          <a href="/prep_tasks/<%= prep_task.id %>" class="btn btn-primary">Show</a>
          <a href="/prep_tasks/<%= prep_task.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_prep_task/<%= prep_task.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
